#include <iostream>
#include <vector>
#include <algorithm>
#include <ctime>
#include <cstdlib>

using namespace std;
/*
int main()
{
	// Creating a list of scores!
	vector<int> scores;
	scores.push_back(1500);
	scores.push_back(3500);
	scores.push_back(7500);

	cout << "High scores:" << endl;

	vector<int>::const_iterator iter;// can't change value by cont_iterator

	int score;
	cout << "Enter a score to find: " << endl;
	cin >> score;
	iter = find(scores.begin(), scores.end(), score);

	if (iter != scores.end())
	{
		cout << "Score found." << endl;
	}
	else
	{
		cout << "Score not found." << endl;
	}

	srand(static_cast<unsigned int>(time(0)));
	// random_shuffle(scores.begin(), scores.end());// shuffle vector
	sort(scores.begin(), scores.end());// sort vector from min to max
	cout << "High Scores:" << endl;
	for (iter = scores.begin(); iter != scores.end(); ++iter)
	{
		cout << *iter << endl;
	}

	return 0;

}*/
