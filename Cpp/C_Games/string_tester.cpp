#include <iostream>
//#include <string>

using std::string;
using std::endl;
using::std::cout;
/*
int main()
{
	string word1 = "Hello";
	string word2(",man");
	string word3(13,'!');// 13 of '!'

	string phrase = word1 + word2 + word3;
	cout << phrase << endl;;

	cout << "phrase size: " << phrase.size() << endl;

	for(unsigned int i=0;i<phrase.size();i++)
	{
		cout << "symbol at " << i << " position is: " << phrase[i] << endl;
	}

	string manWord = "man";
	cout << "word '" << manWord << "' is on position: " <<phrase.find(manWord) << endl;

	phrase[8] = 'Y';
	cout << "new phrase: " << phrase << endl;

	cout << "string::npos : " << string::npos << endl;// if find() find 0 it will be equals to string::npos

	phrase.erase(4,7);
	cout << "after erasing: " << phrase << endl;

	phrase.erase(4);// erase chars from 4 to end
	cout << "after second erasing: " << phrase << endl;

	phrase.clear();
	if(phrase.empty())
		cout << "phrase is clear now" << endl;

	return 0;

}*/
