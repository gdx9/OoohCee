#include <iostream>
#include <fstream>
#include <cstring>

using namespace std;
/*
int main()
{
    ofstream fout;
    //ofstream fout("text.txt");

    // open file for changing
//    //fout.open("text.txt");// REwrite
//    fout.open("text.txt", ios::app);// write to the end
//    fout << "Hello world!" << endl;// we can use it like cout
//    fout << "gdfg" << endl;
    
    ifstream fin;
    fin.open("text.txt");// лишь бы в файле в конце был символ, а НЕ ПРОБЕЛ
    if(!fin.is_open())
    {
        cout << "no such file!" << endl;
        return -1;
    }
    
    //while(fin.eof()){}// run to the end
    
    int a[4];
    
    for(int i{0}; i<4; i++)
    {
        fin >> a[i];
        cout << a[i] << endl;
    }
    
    return 0;
}
*/
